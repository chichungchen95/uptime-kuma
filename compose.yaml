services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 64M
    volumes:
      - ./data:/app/data
    ports:
      - "3001:3001"
